const c = [1].join();

const a = [1, 2, 3, 4, 5].join();

const a1 = ["default-src 'none';", "base-uri 'none';", "object-src 'none';"].join(' ');

const a2 = [s0 ? 5 : 0, 1, 'two', 'three', true, null, undefined, s1, s12].join(a.S);

const a3 = [s1, s12, s13, s14].join('\n' + sep);

const a4 = ["default-src 'none';", "base-uri 'none';", "object-src 'none';"].join(sep);
